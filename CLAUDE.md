# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

**The MVP scaffold exists and the first milestones are implemented.** The Next.js 14 App Router app is set up (TypeScript strict, Tailwind, shadcn/ui, next-intl pt-BR, TanStack Query, Vitest + Playwright) with `supabase/` migrations + seed for the local stack. The commands and structure below are real — verify against the tree, but don't assume the repo is empty.

**Done so far (merged to `main`):**

- **M0 — Fundação:** #0001 bootstrap/tooling, #0002 infra & env (`lib/env.ts` with Zod boot validation), #0003 schema + triggers (`handle_new_user`, `set_updated_at`, `check_product_limit`) + dev seed, #0004 RLS on every user table.
- **M1 — Conta e autenticação:** #0005 auth e-mail/senha (Server Actions, `@supabase/ssr` clients in `lib/supabase/`, session middleware, Upstash login rate-limit, Resend pt-BR templates), #0006 Google OAuth (`/auth/callback`, automatic e-mail linking), #0007 dashboard shell (`app/(dashboard)/` auth guard + `MobileBottomNav`/`DesktopSidebar` + placeholder routes + `useCurrentUser`), #0008 onboarding wizard (`/onboarding`, slug availability via `is_slug_available` RPC, activates the vitrine), #0009 perfil/conta/exclusão LGPD (edição de perfil+vitrine, `ImageUploader` reutilizável + `avatars` bucket, `request_account_deletion`/`anonymize_account`).
- **M2 — Produtos e vitrine:** #0010 CRUD de produtos parte 1 — listagem (`/produtos` com `ProductsManager`), `ProductForm` em `Sheet` (RHF+Zod), upload da foto principal (`products` bucket, reusa `ImageUploader`), `createProductAction` (rate-limit 30/min, checagem de limite Free + trigger `check_product_limit`), modal de upgrade, auto-save de rascunho (localStorage), preços em centavos (`lib/money.ts`). #0011 CRUD parte 2 (4 PRs) — edição/exclusão, "esgotado" (`is_available`) e preço promocional; categorias (sheet de gestão) e marca por autocomplete (`<datalist>` de `suggested_brands` + livres, find-or-create); até 5 imagens (`ProductImagesManager`, capa/ordem/remover, reconciliação de `product_images`); reordenação por drag-and-drop (`@dnd-kit`, `display_order`) + duplicar produto (copia campos e imagens no Storage). #0012 vitrine pública (`app/(public)/[slug]`, Server Component + ISR `revalidate=60`/`generateStaticParams`) em 2 PRs: leitura em `lib/vitrine-data.ts` via clients **sem cookies** (`lib/supabase/public` anon + `admin` service role para o perfil, pois `profiles` não tem RLS pública); `VitrineHeader`/`VitrineGrid` (reusa `ProductCard`)/`ProductDetailModal` com carrossel scroll-snap; tema claro/escuro/auto (`.theme-auto`) + `theme_primary` e `prefers-reduced-motion`; SEO/OG/Schema.org (`generateMetadata` + JSON-LD), `sitemap.xml`/`robots.txt`; o CTA "Pedir no WhatsApp" é um link `wa.me` básico (a lógica completa — mensagem + intent — é #0013). `setRequestLocale` no root layout habilita o render estático/ISR. #0013 botão "Pedir no WhatsApp": `WhatsAppButton` (verde + `WhatsappIcon` SVG inline) no card (slot `action` do `ProductCard`) e no modal, com mensagem pré-formatada por produto (`lib/whatsapp.ts`: nome + preço/promo + link); botão flutuante geral (`VitrineWhatsappFab`, mensagem genérica); desabilita em "esgotado"; e ping **não-bloqueante** `recordOrderIntent` (`lib/intent.ts`, `navigator.sendBeacon`) a `POST /api/intent` no mesmo clique — o endpoint/persistência/rate-limit/hash de IP e o painel de Pedidos são da #0015 (até lá a chamada dá 404 silencioso). #0014 navegabilidade da vitrine: busca + filtros por categoria/marca **client-side** sobre os produtos já carregados (`lib/search.ts`, `VitrineExplorer`; preserva o ISR — estado lido/escrito na URL via `window.location`/`history.replaceState`; full-text GIN do Postgres = otimização futura), empty states (`EmptyState`: vitrine vazia / sem resultado com "limpar filtros"), placeholder "Foto em breve" no `ProductCard`, e `ShareButton` reutilizável (Web Share API + fallback copiar link) no painel (`/produtos`) e na vitrine.
- **M3 — Pedidos e estatísticas:** #0015 (core) intenções de pedido — `POST /api/intent` (anon, sem cookies; rate-limit 10/min por IP + dedup curto por `ip_hash`+`product_id` via Upstash; **SHA-256 do IP** (`hashIp`, nunca o IP cru); user-agent resumido a 3 categorias; `source` derivado do referrer — `lib/intent-source.ts`, `lib/validators/intent.ts`) insere em `order_intents`; o trigger `bump_product_intents` soma `products.intents_count`. Tela **Pedidos** (`app/(dashboard)/pedidos`) com feed agrupado por dia (`lib/intents.ts` + `IntentsFeed`), produto/horário/dispositivo e origem (Pro+); `recordOrderIntent` (#0013) agora envia `referrer`. Contagem de **views** da vitrine (PR2): ping no client (`VitrineViewTracker`/`recordVitrineView`, `lib/view.ts`, dedup por sessão) → `POST /api/view` → RPC `increment_vitrine_views` (dedup curto por IP). #0015 completo. #0016 **painel de Estatísticas** (`app/(dashboard)/estatisticas`, 2 PRs): nova tabela rollup **`vitrine_daily_stats`** (views+cliques/dia, fuso BRT, RLS owner-read) — o RPC `increment_vitrine_views` passou a fazer upsert das views do dia e o trigger `bump_vitrine_daily_intents` conta os cliques do dia (rotas `/api/view` e `/api/intent` inalteradas). PR1: `StatCard`s (views total/7d/30d + cliques 7/30d), ranking de produtos mais procurados, `EmptyState`, helpers puros `lib/stats.ts` (`summarizeStats`/`topProductsByIntents`) e `lib/plan.ts` (`isPaidPlan`), abas `SectionTabs` cruzando Pedidos↔Estatísticas (acesso mobile, já que o bottom nav é fixo em 4 ícones). PR2: gráfico temporal de visitas/cliques em **SVG próprio** lazy via `next/dynamic` (toggle 7/30d), **origem do tráfego** (Pro+ via `isPaidPlan`; Free vê teaser bloqueado + CTA reusando `UpgradeModal`), e banner ao passar de 100 views (`ViewsUpgradeBanner`, gancho de upgrade do PRICING §5.1). #0016 completo.
- **M4 — PWA:** #0017 PWA instalável (4 PRs). PR1: `app/manifest.ts` (display standalone, cores da marca, ícones 192/512/maskable, atalhos "Adicionar produto" → `/produtos?novo=1` e "Ver vitrine" → `/minha-vitrine` route handler), ícones gerados por `scripts/generate-icons.mjs` (sharp) a partir de um monograma temporário (`assets/icon-source.svg`), `appleWebApp`/apple-touch-icon no metadata, e `InstallPrompt` (banner dispensável no shell do dashboard: Android `beforeinstallprompt` / instruções iOS). PR2: **service worker via Serwist** (`@serwist/next`) — `app/sw.ts` com `defaultCache` (NetworkFirst de páginas/RSC → vitrine e painel offline) + CacheFirst das imagens do Supabase Storage; fallback `app/~offline`; `public/sw.js` é gerado no build (gitignored) e **desabilitado em `next dev`**. PR3: **Sentry** (`@sentry/nextjs`) client/server/edge via `instrumentation.ts` + `app/global-error.tsx`, `withSentryConfig` no `next.config.mjs`; scrubbing de PII centralizado em `lib/observability/scrub.ts` (`beforeSend`/`beforeSendTransaction` removem e-mail/telefone/CPF/usuário, preservam stack traces); no-op sem `NEXT_PUBLIC_SENTRY_DSN`. PR4: enxugado o SDK do Sentry no client (`bundleSizeOptimizations`, sem tracing/replay → First Load compartilhado 161→120KB); Lighthouse mobile da landing 100/100; score da vitrine pública verificado manualmente (finaliza na #0024). #0017 completo.

**Routing reality to know:** the `(dashboard)` group is parenthesized (no URL segment), so the panel home is **`/produtos`** — there is no `/dashboard` route. Post-auth redirects go through `DEFAULT_REDIRECT` in `lib/auth/redirect.ts` (= `/produtos`). The auth guard lives in `app/(dashboard)/layout.tsx` (no session → `/login`; `onboarding_completed_at` null → `/onboarding`).

- **M5 — Pagamento e planos:** #0018 **integração Asaas** (3 PRs) — camada de pagamento abstraída em **`lib/payments/`** (interface neutra `PaymentGateway` + `AsaasGateway` cliente HTTP; `PLAN_CATALOG`/`getPlanEntry`/`planFromValueCents`; trocar de gateway = trocar a impl, não os call-sites). `POST /api/checkout` (autenticado) cria cliente+assinatura no Asaas e grava ids/período via RPC `attach_asaas_checkout` (RLS de `subscriptions` é select-only); CPF/CNPJ **forward-only** (nunca persistido). Tela `/assinar` redireciona à **página hospedada do Asaas** (PIX/cartão/boleto). `POST /api/webhooks/asaas` autentica pelo header `asaas-access-token` (`timingSafeEqual`, **não** é HMAC do corpo), idempotência via `payment_webhook_events` (`event_id` UNIQUE, gravado antes do efeito; desfeito em falha p/ retry), faz upsert em `invoices` e ativa o plano (reverse-lookup do valor) — único handler com **service role**. #0019 **gestão de plano** (6 PRs): `PlanComparisonTable` (Free/Pro/Plus, dados em `lib/plan-features`) + toggle **mensal/anual (-20%)** na `/assinar` + avisos "5 de 5" no painel; tela **"Meu plano"** (`/conta/plano`, abas `ContaTabs`) com status/período/próxima cobrança e **histórico de faturas com PDF**; `PlanWelcome` (toast + tour pós-upgrade, flag por plano no localStorage); **cancelamento self-service** + **garantia de 7 dias** (refund automático via `gateway.refundPayment` → RPC `downgrade_to_free`) + pesquisa de saída (`request_subscription_cancel`, grava em `audit_logs`); **cupons** (`validate_coupon`/`redeem_coupon` SECURITY DEFINER + seed `PRIMEIRA50`/`ANUAL30`/`INDICACAO`; desconto aplicado **só na 1ª cobrança** no checkout, `lib/coupons`); **inadimplência/expiração** (`subscriptions.past_due_since`; vitrine esconde o excedente ao vivo após 14d via `lib/billing/limits`; `POST /api/cron/billing` rebaixa para Free aos 30d via `lib/billing/transitions`; agendado por **pg_cron+pg_net**, migration guardada/secretless via GUC, env `CRON_SECRET`) — **nunca apaga produtos**; **e-mails transacionais** via Resend (`lib/email/`, REST sem SDK, no-op sem chave): confirmação de assinatura, recibo de fatura e dunning **D1/D3/D7**, disparados pelo webhook e pelo cron. As tabelas `subscriptions`/`invoices`/`coupons`/`coupon_redemptions` + enums já existiam desde o schema inicial (#0003). #0020 **programa de indicação** (3 PRs): cada usuária **Pro+** tem um link `?ref=<code>` (código opaco em `profiles.referral_code`, gerado sob demanda por `ensure_referral_code`); a captura do `?ref=` vive num **cookie httpOnly** setado no middleware (o link aponta para a raiz) e consumido no cadastro por e-mail (metadata → trigger `handle_new_user`) ou no callback OAuth (RPC `apply_referral`), dando à **indicada 30 dias de Pro (trial)** em vez de Free; quando a indicada vira pagante Pro+, o webhook do Asaas marca `referrals.converted_at` e **adia em 1 mês a próxima fatura** de quem indicou (`reward_granted`, via `getNextPendingPayment`+`updatePayment` — defer direto, **não** usa o cupom `INDICACAO`, que segue semeado p/ uso manual/marketing); o `POST /api/cron/billing` passou a **expirar trials vencidos** (`downgrade_trial_expired`) e a enviar o **nudge de 30 dias** ("indique uma amiga", idempotente via `profiles.referral_nudge_sent_at`); painel **`/conta/indicacoes`** (aba `ContaTabs`, Pro+) com link compartilhável (`ShareButton`), contadores (`StatCard`) e lista **anonimizada** — Free vê gate de upgrade. A `referrals` foi **reestruturada para por-evento** (saiu o `UNIQUE NOT NULL` de `code`; o código por-usuária mora em `profiles.referral_code`). Lógica pura em `lib/referrals.ts` (`decideReferralReward`/`summarizeReferrals`) e `lib/payments/dates.ts`.

- **M6 — Conformidade e polimento:** #0021 **conformidade legal e LGPD** (5 PRs): páginas legais públicas `/termos`/`/privacidade`/`/cookies` (drafts pt-BR "a revisar por advogado", `components/legal/`, casca `LegalPage`); links legais nos rodapés (landing, painel `app/(dashboard)/layout`, e `VitrineFooter`) via `lib/legal/links`; **aceite versionado** dos Termos (`profiles.terms_version`/`terms_accepted_at`, `TERMS_VERSION` em `lib/legal/version`) — checkbox no cadastro e-mail/senha (metadata → `handle_new_user`) e **gate `TermsGate` no onboarding** para OAuth/reaceite (`acceptTermsAction`). **Auditoria automática**: `log_audit_event()` por triggers em `products`/`vitrines`/`profiles`/`subscriptions` (UPDATE só em campos materiais, sem ruído de contadores) grava `audit_logs` (actor/action/entity/metadata); `ip_hash` via GUC `app.audit_ip_hash` (transação) + evento `auth.login` (`record_login_audit` no `loginAction`). **Retenção** (`lib/retention/windows` + `POST /api/cron/retention`, pg_cron): apaga `audit_logs` >180d, anula `ip_hash` de `order_intents` >12 meses, **anonimiza contas 30d** (`anonymize_account`, `profiles.anonymized_at`) e **exclui 90d** (`hard_delete_account` → `DELETE auth.users`), preservando o fiscal (5 anos) em **`invoice_archive`** (campos não-PII). E-mails (Resend): confirmação de exclusão (com prazos), anonimização e exclusão. **Direitos do titular**: tela **"Meus dados"** (`/conta/dados`) com export `GET /api/conta/export` (pessoais JSON + produtos CSV/JSON via `lib/export/to-csv`) e **"Privacidade"** (`/conta/privacidade`, `profiles.marketing_opt_in` revogável; o nudge de indicação no cron passa a respeitá-lo). Plano de incidentes em `docs/security-incidents.md`. Itens operacionais (caixas `dpo@`/`direitos@`, revisão jurídica, NF-e) ficam no checklist do #0025; sistema de denúncia é #0023; backups testados/a11y são #0024.

  #0022 **suporte e conteúdo** (2 PRs): **Central de Ajuda pública `/ajuda`** (Server Component estático) com **FAQ pesquisável** (24 perguntas pt-BR em `lib/help/faq.ts`, busca acento-insensível via `filterFaq`/`normalize`, perguntas em `<details>` por categoria) e **tutoriais em vídeo** (`lib/help/tutorials.ts`, `VideoEmbed` com **facade** YouTube — iframe `youtube-nocookie` só carrega ao clicar, sem cookies antes; `youtubeId` nulo → "em breve"); **suporte via WhatsApp** (`SupportCta` + `buildWhatsappUrl`, envs `NEXT_PUBLIC_SUPPORT_WHATSAPP`/`_SUPPORT_EMAIL`/`_INTRO_VIDEO_ID`); pontos de acesso "Ajuda" no rodapé legal, sidebar desktop e card em `/conta`. **Primeiro acesso**: `completeOnboardingAction` redireciona a `/produtos?bemvinda=1` e o `WelcomeTour` (montado no shell via `Suspense`, padrão do `PlanWelcome` com flag no localStorage) mostra uma vez o vídeo intro de 60s + 3 dicas/CTAs (decisão: card, não spotlight). "Suporte prioritário" do Plus já existia em `lib/plan-features.ts`. Conteúdo a produzir (gravar os vídeos + ids, número do WhatsApp de suporte) é operacional → #0025.

  #0023 **administração interna e moderação** (4 PRs): área **`app/admin/`** (fora do grupo `(dashboard)`) com guarda dupla no `layout` — sem sessão → `/login`; e-mail fora de `ADMIN_EMAILS` → 404 neutro (`lib/admin/guard` `getAdminUser`/`isAdminEmail`); toda leitura/escrita admin usa `createAdminClient()` (service role) só no servidor. **Contas** (busca + detalhe, `lib/admin/accounts`), **audit_logs** com filtros (`lib/admin/audit`). **Bloqueio de moderação** (`blocked_at`/`blocked_reason` em `vitrines` e `profiles`): a policy `vitrines_public_read` exige `blocked_at IS NULL`, e o trigger `protect_block_fields` (SECURITY INVOKER) só deixa `service_role` alterar o bloqueio — a dona não se desbloqueia; vitrine bloqueada vira página neutra no público (`getVitrineModerationState`), conta suspensa cai em `/login?erro=conta-suspensa`. **Denúncia DMCA**: tabela `reports` (RLS sem policy), botão "Denunciar" → `/denunciar` → `POST /api/report` (anon, rate-limit 5/h, `checkReportRateLimit`) grava + notifica `direitos@` (`reportFiledEmail`); gestão em `/admin/denuncias` (filtro por status, SLA 48h, ocultar vitrine/resolver/descartar, auditado). **Métricas** (`/admin/metricas`): RPC `admin_metrics()` SECURITY DEFINER (service-role-only) — DAU/MAU por atividade distinta em `audit_logs` + funil cadastros→onboarding→≥1→≥5 produtos→plano pago (`lib/admin/metrics`). **Health check** (`/admin/health` + `GET /api/admin/health`): DB/Storage com ping real, pagamento/e-mail por configuração (`lib/admin/health`). Alertas Slack/e-mail (§8.4) → #0024; selo embaixadora → #0025.

  #0024 **polimento, acessibilidade, E2E, backup, alertas** (6 PRs): **a11y/`alt`** — `product_images.alt_text` agora é capturado por foto no `ProductImagesManager` (fallback p/ o nome do produto na leitura); auditoria **axe** (`tests/e2e/a11y.spec.ts`, `@axe-core/playwright`) corrigiu contraste AA (token `--muted-foreground` 46→42%) e links in-text (sublinhado permanente). **i18n** — strings das telas públicas (welcome-tour, video-embed, report-form, ajuda, páginas de erro/offline) movidas para `messages/pt-BR.json` (admin interno e prosa legal mantêm literais por decisão). **E2E** — 5 fluxos críticos com Playwright contra o **Supabase local** (`.env.test` + webServer com `NODE_ENV=test`, p/ o Next ignorar o `.env.local` remoto e levar as `NEXT_PUBLIC` ao cliente; helper pg + `loginAs` + `uploadProductPhoto`), job `e2e` no CI (sobe Supabase + seed). Os E2E acharam **3 bugs reais** (corrigidos): `lib/env` montava `clientEnv` com `safeParse(process.env)` — o objeto bare fica sem as `NEXT_PUBLIC_*` no bundle do cliente (agora acessos diretos); e a listagem de produtos (SSR de `/produtos` + `useProducts`) não filtrava por `vitrine_id`, vazando produtos de outras vitrines + inflando o limite Free (agora escopado). **Alertas** — só e-mail (decisão; sem Slack): `lib/alerts` (notifyAdmins → `ADMIN_EMAILS`) + contador Upstash p/ falha 3x do webhook Asaas + `POST /api/cron/health` (pg_cron 15min); p95/5xx/Storage ficam como regras de plataforma (ARCHITECTURE §8.4). **Backup** — `.github/workflows/backup.yml` (dump semanal `pg_dump`→gzip→S3/Wasabi, 90d) + runbook `docs/BACKUP.md` (restore + RTO 4h/RPO 24h + checklist de teste mensal; teste ao vivo é operacional/go-live). **ADRs** 001–005 em `docs/adr/`. **NF-e** e **stress test (k6)** → movidos para **#0025**.

**Not built yet:** #0025 (beta fechado com embaixadoras + go-live; NF-e automática, stress test, transações reais — operacional/lançamento). Obs.: a pasta `lib/asaas/` permanece um `.gitkeep` vazio — a implementação real vive em `lib/payments/` (decisão de abstração de gateway).

The canonical decisions are in `docs/ARCHITECTURE.md` (§3 stack, §4 folder structure) and `docs/ROADMAP.md` (build order). Work is broken into ~25 numbered issues under `issues/` (see `issues/README.md` for the index + SPEC→issue map) — pick one up there and respect its **Depende de** field. Treat `docs/` as the source of truth and keep both `docs/` and the relevant `issues/*.md` updated in the same change that alters behavior or schema.

> Note: `.env.local` currently points at the **remote** Supabase project, so end-to-end checks of authenticated flows are usually done manually; automated tests cover validators, components, and unauthenticated redirects, with the local Supabase stack (`supabase start`) used for DB-level checks.

## What this product is

A PWA where Brazilian multi-brand direct-sales resellers (Avon + Natura + Hinode + Mary Kay, etc.) build one shareable digital storefront ("vitrine") at `app-domain/their-slug`. The public storefront lists products (photo, name, price); each product has a **"Pedir no WhatsApp"** button that records an order-intent and opens WhatsApp with a pre-filled message. The sale itself is closed in WhatsApp — the app never handles a cart or checkout for end-customers. Monetization is a recurring SaaS subscription (Free / Pro / Plus) billed via Asaas (PIX/card/boleto). All UI copy is **pt-BR**.

> Naming note: the product is **Vitrinio** (domain `vitrinio.com.br`, already acquired). The repo folder is still named `vitrify` for historical reasons — don't rename it. In code/configs use `vitrinio` (e.g. `package.json` name, `NEXT_PUBLIC_APP_NAME=Vitrinio`, `SENTRY_PROJECT=vitrinio`).

## Documentation map

| File                   | Read when you need…                                                                                                                         |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| `docs/README.md`       | One-paragraph overview, stack summary, prerequisites                                                                                        |
| `docs/SPEC.md`         | Problem, solution, product principles, personas, MVP scope, success metrics                                                                 |
| `docs/FEATURES.md`     | Feature list with MoSCoW priority and Free/Pro/Plus split                                                                                   |
| `docs/DESIGN.md`       | Design tokens (colors, type, spacing, radii), component list, UX patterns, key user flows                                                   |
| `docs/ARCHITECTURE.md` | Stack, system diagram, ADR-worthy decisions, folder structure, critical flows, security, perf budgets                                       |
| `docs/DATABASE.md`     | Postgres schema (all tables/columns), triggers, RLS policies, migration strategy, seed plan                                                 |
| `docs/ROADMAP.md`      | Phased MVP plan with per-week deliverables and done-criteria                                                                                |
| `docs/CONTRIBUTING.md` | Branch/commit/PR conventions, code style, testing strategy, security checklist                                                              |
| `docs/PRICING.md`      | Plan pricing, why Asaas over Stripe, conversion strategy                                                                                    |
| `docs/LEGAL.md`        | LGPD obligations, IP/brand-image risk, terms, account deletion                                                                              |
| `docs/GTM.md`          | Acquisition channels and launch plan                                                                                                        |
| `issues/README.md`     | Backlog index: ~25 issues (`issues/NNNN-slug.md`) grouped into milestones M0–M7, with dependencies and a map from `SPEC.md` §6/§8 to issues |

## Planned architecture (the big picture)

Fullstack monolith on **Next.js 14 App Router** (TypeScript) deployed to **Vercel**, with **Supabase** (Postgres 15 + Auth + Storage + Edge Functions) as the backend, and **Asaas** as the payment gateway (webhook-driven). Email via Resend; rate limiting via Upstash Redis; errors via Sentry; analytics via Plausible.

Three route groups under `app/`:

- `(auth)/` — login, cadastro, recuperar-senha (Supabase Auth, Google OAuth)
- `(dashboard)/` — the reseller's panel: produtos, pedidos, estatisticas, conta (mobile-first, bottom nav)
- `(public)/[slug]/` — the public storefront, rendered as a **Server Component with ISR (`revalidate` 60s)**; product edits call `revalidatePath('/<slug>')`

API surface under `app/api/`: `webhooks/asaas` (HMAC-validated, idempotent by event id), `intent` (public, rate-limited, non-blocking write of order-intents). Shared code in `lib/` (`supabase/` server+browser clients, `asaas/` client, `validators/` Zod schemas shared client↔server, `analytics/`), `components/ui/` (shadcn) + `components/{product,vitrine,shared}/`, `supabase/migrations/` for versioned SQL.

### Flows that span multiple files (know these before editing)

- **Product create**: client compresses image (browser-image-compression → ~webp, ≤1200px, ≤500KB) → direct upload to Supabase Storage via signed URL → `POST /api/products` with the URL → server validates with Zod, enforces plan limit, inserts → TanStack Query cache update.
- **Order intent**: product button → non-blocking `POST /api/intent` (slug, product id, source/referrer, short UA, **SHA-256 hash of IP** — never the raw IP) → in parallel redirect to `wa.me/<phone>?text=<message>`. Owner sees intents in the "Pedidos" panel.
- **Recurring payment**: "Assinar Pro" → `/api/checkout` creates Asaas customer+subscription, returns payment link → user pays (PIX QR / card / boleto) → Asaas webhook → `/api/webhooks/asaas` validates HMAC, upserts `subscriptions` + `invoices` → next request reflects new plan/limits.

### Data model essentials

`profiles` (1:1 with `auth.users`, auto-created by `handle_new_user` trigger which also seeds a `free` `subscriptions` row and an inactive `vitrines` row) → `vitrines` (slug-keyed, `is_active` gate for public visibility) → `products` (+ `product_images`, `categories`, `brands` — all per-vitrine) and `order_intents`; plus `subscriptions` → `invoices`, and `audit_logs` / `referrals`. **Prices are stored as integer cents** (`price_cents`, `promo_price_cents`); formatting to `R$ 32,90` happens only in the presentation layer. Postgres full-text search on products via a generated `search_text` tsvector (Portuguese config). Plan-limit enforcement is both a DB trigger (`check_product_limit`) and an app-layer check.

## Engineering conventions (from `docs/CONTRIBUTING.md`)

- **Package manager: pnpm.** Node 20+.
- **Commits: Conventional Commits, description in Portuguese** — `feat(produto): permite cadastrar até 5 imagens`. Types: `feat fix chore docs refactor test style perf`.
- **Branches**: `main` (→ prod), `staging` (→ staging), `feat/…`, `fix/…`, `chore/…`, `docs/…`. Everything via PR; keep PRs under ~500 changed lines.
- **TypeScript strict, no `any`** (use `unknown` + validate). Shared types in `types/`; shared Zod schemas in `lib/validators/`.
- **Server Components by default**; add `"use client"` only for state/effects/browser-event/browser-API/client-only-lib needs.
- **Named exports only** for components (Next pages are the exception). One component per file. File names kebab-case (`product-card.tsx`), components PascalCase, hooks `useXxx`, SQL tables/columns snake_case.
- **Tailwind only** (no loose CSS outside `globals.css`); semantic tokens (`brand-primary`, not `purple-600`) in `tailwind.config.ts`; `cn()` for conditional classes; `cva` for variants. Design tokens are enumerated in `docs/DESIGN.md`.
- **Forms**: React Hook Form + Zod resolver; disable submit while `isSubmitting`; toast on success.
- **DB access**: typed Supabase client (`supabase gen types`); server client in `lib/supabase/server.ts` for server code; browser client wrapped in TanStack Query hooks for client code; state-changing mutations go through Server Actions / Route Handlers, not the browser client directly. Always select explicit columns (never `SELECT *`), always `LIMIT` lists.
- **Security**: RLS on every table with user data (service role only in webhook/admin routes, never shipped to client); validate every input server-side with Zod; sanitize user HTML/Markdown before render; block SVG uploads; validate slug against a reserved-word blacklist; never log email/phone/CPF/payment data; webhook HMAC + idempotency always.
- **`.env`**: keep `.env.example` in sync; validate env at boot with Zod in `lib/env.ts` (build fails if missing); `NEXT_PUBLIC_` prefix only for values that may reach the browser.
- **Migrations** in `supabase/migrations/` named `YYYYMMDDHHMMSS_descricao.sql`, append-only (never edit an applied one), idempotent where possible, destructive changes done in two steps across separate PRs.

## Commands

```bash
pnpm install
pnpm dev                       # Next.js dev server on :3000
pnpm build && pnpm start       # production build + serve
pnpm lint                      # ESLint
pnpm typecheck                 # tsc --noEmit  (verify exact script name in package.json)
pnpm test                      # Vitest (unit/integration) — *.test.ts / *.spec.ts beside the code
pnpm test -- <path>            # run a single test file
pnpm test:e2e                  # Playwright (critical flows only — see CONTRIBUTING §3.2)

supabase start                 # local Postgres/Auth/Storage via Docker
supabase db push               # apply migrations
supabase gen types typescript --local > types/supabase.ts   # regenerate DB types after schema changes
```

CI (GitHub Actions, `.github/workflows/ci.yml`) runs typecheck + lint + unit tests + E2E-on-preview; production deploy is a manual promotion during the MVP. These scripts exist in `package.json` today; the Vitest suite enables JSX via `@vitejs/plugin-react` (see `vitest.config.ts`). Note: the Playwright browser may need `pnpm exec playwright install chromium` in a fresh environment.
